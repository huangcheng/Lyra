//! OpenGPG (OpenPGP wire format) — key store foundation.
//!
//! Library: **rPGP** (`pgp` crate) — pure Rust, MIT/Apache (opengpg-spec P1).
//! Migration: `0008_opengpg_keys` (0007 already used for folder role overrides).

pub mod keys;
pub mod store;
